package main

import "fmt"

func main() {
    // Define an array of integers and assign values to its elements
    var numbers [5]int
    numbers[0] = 1
    numbers[1] = 2

    // Print the array
    fmt.Println(numbers)
}
// Expected Output: [1 2 0 0 0]
